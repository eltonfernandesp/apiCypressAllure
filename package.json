{
  "dependencies": {
    "cypress": "^12.12.0"
  },
  "scripts": {
    "cy:run": "cypress run",
    "cy:report": "allure generate allure-results --clean -o allure-report",
    "cy:allure": "npm run cy:run && npm run cy:report"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.39.1",
    "allure-commandline": "^2.22.0",
    "cypress-multi-reporters": "^1.6.3",
    "mocha-allure-reporter": "^1.4.0"
  }
}
